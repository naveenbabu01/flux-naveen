apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-k8s-ops-agent
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8501
      protocol: TCP
  selector:
    app: k8s-ops-agent
