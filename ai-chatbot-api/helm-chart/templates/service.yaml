apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-chatbot-api.fullname" . }}
  labels:
    {{- include "ai-chatbot-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ai-chatbot-api.selectorLabels" . | nindent 4 }}
